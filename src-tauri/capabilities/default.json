{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Shared capability for all windows",
  "windows": ["main", "photo-vault"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
